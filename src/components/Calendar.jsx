import React from 'react';

const Calendar = () => (
    <section id="events">
        <h2>Events Calendar</h2>
        <div id="calendar">Calendar will be here</div>
    </section>
);

export default Calendar;
